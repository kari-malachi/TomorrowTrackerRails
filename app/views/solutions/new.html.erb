<div id='page_title'>
  <h1>Add a new solution</h1>
</div>
<%= render 'shared/errors', container: @solution %>
<%= render 'form', button_msg: 'Add Solution' %>
<%= link_to 'Cancel', show_clue_path(stack_id: @stack, id: @clue) %>
